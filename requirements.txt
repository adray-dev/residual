# Runtime dependencies for the API when it is deployed as a Vercel Python Function.
#
# Vercel installs from this file; it does not resolve the `api` extra in pyproject.toml,
# which is why this list exists separately rather than being derived from it. Keep the two
# in step: this is exactly `[project] dependencies` plus the `api` extra, minus uvicorn
# (Vercel invokes the ASGI app directly, so no server is needed) and minus everything in
# the `data` extra (geopandas/shapely/pyproj/SQLAlchemy are loader-only and are most of
# the install size).
#
# Local development is unchanged and still uses `pip install -e ".[dev]"`.

# Engine — pure computation, no I/O.
numpy>=1.26
numpy-financial>=1.0
scipy>=1.11

# API surface.
fastapi>=0.110
openpyxl>=3.1          # the live-formula Excel export (api/workbook.py)

# Database access, used only by data/repositories.py.
psycopg[binary]>=3.1
psycopg-pool>=3.2
python-dotenv>=1.0
